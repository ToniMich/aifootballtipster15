import React from 'react';

// This component's content has been removed as per user request.
// The banner is no longer needed for the application.
const ServiceDisruptionBanner: React.FC<{ isVisible: boolean; onClose: () => void; }> = () => null;

export default ServiceDisruptionBanner;
