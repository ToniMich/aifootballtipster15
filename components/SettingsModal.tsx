import React from 'react';

// This component is no longer needed. API keys are managed securely on the
// server via Supabase Edge Functions, so there is no need for users to configure
// them on the client side.
const SettingsModal: React.FC = () => null;

export default SettingsModal;