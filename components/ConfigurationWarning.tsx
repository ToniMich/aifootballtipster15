import React from 'react';

// This component is no longer used. The main App component has been updated
// to allow functionality even when the backend is not configured, to facilitate
// easier debugging of service-layer errors.
const ConfigurationWarning: React.FC = () => null;

export default ConfigurationWarning;